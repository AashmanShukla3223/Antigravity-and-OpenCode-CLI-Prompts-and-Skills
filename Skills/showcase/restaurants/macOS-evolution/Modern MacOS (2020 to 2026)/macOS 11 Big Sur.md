# CALL SKILL: /caveman-full-stack-developer

# SYSTEM OVERRIDE: 2% CONTEXT TAX
!! Talker mode = OFF. 
!! English = Expensive. Code = Free.
!! Zero preamble. Build the Overhaul Era.

# PROJECT: macOS 11 Big Sur "The New Foundation"
Build the 2020 environment. Focus on the mobile-first redesign and the high-translucency materials.

# MATERIAL SYSTEM: THE BOLD SHIFT
1. **The Floating Dock**: The Dock is no longer attached to the bottom edge. It is a detached, rounded-corner "pill" floating above the wallpaper.
2. **Control Center**: A unified panel for Wi-Fi, Bluetooth, and Brightness using the "Module" design (rounded rect containers).
3. **Symbolic Icons**: Transition to SF Symbols for all toolbar items.
4. **The Squircle**: Every icon must fit a consistent "App Icon" shape with a 22.5% corner radius.

# TRIGGER
Build the macOS 11 Big Sur Environment. Include the Floating Dock and Control Center modules.

# THE CSS INJECTION (FORCE THIS CODE)
```css
/* Big Sur Floating Dock */
.floating-dock {
  background: rgba(255, 255, 255, 0.35);
  backdrop-filter: blur(30px) saturate(160%);
  border: 0.5px solid rgba(255, 255, 255, 0.2);
  border-radius: 24px;
  padding: 8px;
  margin-bottom: 10px; /* Force the floating effect */
  display: flex;
  gap: 12px;
}

/* Big Sur Module System */
.control-module {
  background: rgba(255, 255, 255, 0.5);
  border-radius: 14px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
  padding: 12px;
}

/* The Iconic Big Sur Shadow */
.window-big-sur {
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
  border-radius: 12px;
}
