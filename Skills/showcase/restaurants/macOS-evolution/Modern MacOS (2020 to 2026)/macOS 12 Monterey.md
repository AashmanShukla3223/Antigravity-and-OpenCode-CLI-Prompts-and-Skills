# CALL SKILL: /caveman-full-stack-developer

# SYSTEM OVERRIDE: 2% CONTEXT TAX
!! Talker mode = OFF. 
!! English = Expensive. Code = Free.
!! Zero preamble. Build the Refinement Era.

# PROJECT: macOS 12 Monterey "Universal Era"
Build the 2021 environment. Focus on the Compact Safari Tab bar and the AirPlay-to-Mac UI.

# MATERIAL SYSTEM: THE BLENDED UI
1. **Safari Compact Tabs**: The top bar of the browser must change its background color to match the primary color of the website being viewed.
2. **Shortcuts Integration**: UI for the "Shortcuts" app icons—vibrant multi-color gradients on rounded-rect tiles.
3. **Focus Modes**: A revamped Control Center with a prominent "Focus" button that changes state with a smooth morphing animation.

# TRIGGER
Build the macOS 12 Monterey Environment. Include the Color-Matching Safari Bar and Focus Mode UI.

# THE CSS INJECTION (FORCE THIS CODE)
```css
/* Monterey Adaptive Tab Bar */
.safari-toolbar {
  background: var(--site-color, rgba(255, 255, 255, 0.5));
  backdrop-filter: blur(20px);
  transition: background 0.8s ease; /* Smooth color-matching transition */
  display: flex;
  align-items: center;
  padding: 0 12px;
}

.compact-tab {
  background: rgba(0, 0, 0, 0.05);
  border-radius: 10px;
  margin: 4px;
  padding: 4px 12px;
  font-size: 13px;
}

/* Shortcuts App Icon Gradient */
.shortcut-tile {
  background: linear-gradient(135deg, #007aff, #5856d6);
  box-shadow: 0 4px 10px rgba(88, 86, 214, 0.3);
}
