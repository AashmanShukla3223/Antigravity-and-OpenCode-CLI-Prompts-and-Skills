# CALL SKILL: /caveman-full-stack-developer

# SYSTEM OVERRIDE: 2% CONTEXT TAX
!! Talker mode = OFF. 
!! English = Expensive. Code = Free.
!! Zero preamble. Zero "I've updated the styles."

# PROJECT: macOS 26 Tahoe "Liquid Glass" Core
Build the definitive 2026 Desktop Environment using Next.js 15 and Tailwind.

# MATERIAL SYSTEM: LIQUID GLASS (LG)
1. **Lensing Effect**: Sidebars and Toolbars must use `backdrop-filter: blur(40px) saturate(210%)`.
2. **Edge Light**: Windows need a 1px border that mimics a "virtual ring light" (Edge Light feature).
3. **Refraction**: Use a subtle `scale-[1.02]` on background content viewed through the glass layer.
4. **Transparent Menu Bar**: 100% clear. Text uses `drop-shadow-[0_1.2px_1.2px_rgba(0,0,0,0.8)]` for 2026 legibility standards.

# COMPONENTS TO SHIP
- `DesktopContainer.tsx`: Handles the dynamic "Tahoe Blue" background.
- `LiquidDock.tsx`: Fully transparent Dock with Apple's new "shimmer" effect on hover.
- `ControlCenter.tsx`: Refined LG layout with customizable "Control" pods.
- `FinderWindow.tsx`: Using the `edge-light` CSS class for the border glow.

# TRIGGER
Build the macOS 26 Tahoe Environment. Code only.

# THE CSS INJECTION (REQUIRED)
```css
.liquid-glass {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(50px) saturate(200%);
  border: 0.5px solid rgba(255, 255, 255, 0.3);
  box-shadow: inset 0 0 12px rgba(255, 255, 255, 0.05);
}
.edge-light {
  box-shadow: 0 0 0 1px rgba(255,255,255,0.5), 0 0 20px rgba(255,255,255,0.25);
}
